x-pipeline: &pipeline-base
  build:
    context: .
    dockerfile: docker/Dockerfile
  image: ds-pipeline
  network_mode: host
  volumes:
    - .:/workspace
  working_dir: /workspace
  environment:
    - PYTHONPATH=/workspace
    - NVIDIA_VISIBLE_DEVICES=all
    - NVIDIA_DRIVER_CAPABILITIES=compute,video,utility
  stdin_open: true
  tty: true
  deploy:
    resources:
      reservations:
        devices:
          - driver: nvidia
            count: all
            capabilities: [gpu]

services:
  pipeline:
    <<: *pipeline-base
    command: ["python3", "pipelines/multi_stream.py",
              "--uri", "rtsp://localhost:8554/stream0",
              "--uri", "rtsp://localhost:8554/stream1",
              "--uri", "rtsp://localhost:8554/stream2",
              "--restream-base-port", "8556",
              "--output-dir", "/workspace"]

  # dashboard:
  #   build: .
  #   network_mode: host
  #   volumes:
  #     - .:/workspace
  #   working_dir: /workspace
  #   command: ["python3", "-m", "metrics.dashboard"]
  #   depends_on:
  #     - pipeline
